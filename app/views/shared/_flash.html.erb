<% flash.each do |name, msg| %>
    <% if msg.is_a? Array %>
        <div class="alert <%= name %>">
            <ul>
                <% msg.each do |message| %>
                <li><%= message %></li>
                <% end %>
            </ul>
        </div>
    <% else %>
        <%= content_tag :div, msg, class: "alert #{name}" %>
    <% end %>
<% end %>